<div class='alert text-bg-success text-center ms-1'>
  {{#with service}}
    <h2>{{title}}</h2>
    <p>{{description}}</p>
    {{#each buttons}}
      <a
        href='{{link}}'
        rel='{{rel}}'
        class='btn btn-primary'
      >{{text}}</a>
    {{/each}}
  {{/with}}
</div>